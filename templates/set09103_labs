from flask import Flask, render_template
app = Flask(__name__)

@app.route('/')
def root():
  return render_template('week10.html'), 200

if __name__ == "__main__"), 200
 app.run(host='0.0.0.', debug=true) 
